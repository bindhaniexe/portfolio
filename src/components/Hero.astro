---
/**
 * Hero.astro — wraps the animated React HeroSection component.
 *
 * The React component handles the parallax floating images, text-rotate
 * keyword animation, and motion entrance animations. It is hydrated
 * client-side so all interactivity works in the browser.
 */
import HeroSection from "./HeroSection";
---

<HeroSection client:only="react" />
